<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Off deliberately: the icons are hand-drawn Canvas paths, which Compose does not
         mirror, so a mirrored layout points them the wrong way. See docs/design-spec-home.md. -->
    <application
        android:name=".TierYourLifeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.TierYourLife">
        <!-- All three, or the activity restarts on a language change anyway: the
             layout-direction bits live inside screenLayout, so the system reports it
             changed too. Verified on a device; dropping any one brings the blink back. -->
        <activity
            android:name=".MainActivity"
            android:configChanges="locale|layoutDirection|screenLayout"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TierYourLife"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
