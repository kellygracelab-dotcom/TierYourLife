package com.artiuillab.tieryourlife.feature.tier.domain.model

// Belongs to one list, not the app as a whole — see TierList.displayMode.
enum class TierListDisplayMode {
    WRAP,
    HORIZONTAL_SCROLL,
    FLAT_RANKED,
}
